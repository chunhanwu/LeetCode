/**
 * @param {string[]} strs
 * @return {string}
 */
let input =  ['flower','flow','flight'];
function longestCommonPrefix (strs) {
    let same = '';
    console.warn('strs',strs)
    return same
};

console.warn('input',input);
console.warn('output',longestCommonPrefix(input));
